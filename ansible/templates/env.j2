# Fichier .env généré par Ansible — ne pas modifier manuellement
# Playbook : deploy-app.yml

# ── API Keys ──────────────────────────────────
OPENAI_API_KEY={{ openai_api_key | default('') }}
ANTHROPIC_API_KEY={{ anthropic_api_key | default('') }}
GEMINI_API_KEY={{ gemini_api_key | default('') }}

# ── Versions ──────────────────────────────────
NGINX_VERSION=1.25.4-alpine
LITELLM_VERSION=main-v1.35.8

# ── Réseau ────────────────────────────────────
DOMAIN_NAME={{ domain_name }}
LETSENCRYPT_EMAIL={{ letsencrypt_email }}

# ── Images applicatives ──────────────────────
# Registry public : ghcr.io/dis-bzh/llm-shield
GITHUB_REPOSITORY=dis-bzh/llm-shield
IMAGE_TAG={{ image_tag | default('latest') }}
